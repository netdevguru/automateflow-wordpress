# Build output — the uploadable ZIP is regenerated by build.sh, never committed.
/dist/

# Local PHPCS / Composer tooling, if installed into the repo.
/vendor/
composer.lock
.phpcs-cache
.phpunit.result.cache

# macOS
.DS_Store
